package _03_looping_and_branching_conditional_statements.conditionals.ii_ternary;

public class TernaryOperator_2 {
    public static void main(String[] args) {
        /*
            Exercise:
            Declare an integer `a` and set its value to 25
            Use a ternary operator to
            determine whether `a` is even or odd.
            Print the result.
            Run the program a few times,
            each time change the int value.
         */
    }
}


































/*

        int a = 25;
        String result = (a % 2 == 0) ? "a is even" : "a is odd";
        System.out.println(result);
 */
